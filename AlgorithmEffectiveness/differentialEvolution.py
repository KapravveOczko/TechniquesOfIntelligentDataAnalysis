import population

'''
function = function to optimalize
iters = number of iterations
size = size of a vector
num = number of entities in population
a, b, = range of function
'''

def DE(function, iters, size, num, a, b):
    pop = population.Population(a, b, num, size)
    best = 0
    for i in range(iters):

    return 0